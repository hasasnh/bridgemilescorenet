@{


    foreach (var ClientCustom in Model.Items)
    {
        var x = ClientCustom;
    }

    foreach (var ClientPartCustom in Model.ContentItem.Content.ClientPartCustom.ContentItems)
    {
        var x = ClientPartCustom;
    }

}